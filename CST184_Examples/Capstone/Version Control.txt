Version Control Summary — 08/02/2026 (AWST)

Project: Capstone — Project 2 (Climate Data Analysis)
Notebook: Project 2.ipynb

-------------------------------------------------------------------------------
CHANGES COMPLETED
-------------------------------------------------------------------------------
8-2-2026
Step 1 — Data importing and cleaning
• Loaded climate dataset: owid_climate_data.csv into main_df using pandas.
• Performed initial exploration: head(), index, columns, info, and null check (isnull()) on the 26,182-row dataset.
• Dropped rows with missing values in "CO2 Emissions Per Capita" and "Annual Temperature Anomaly".
• Clean dataset size: 14,319 rows × 5 columns (Country Name, Country Code, Year, CO2 Emissions Per Capita, Annual Temperature Anomaly).

Step 2 — Sets and functions
• Built country set: extracted "Country Name" and used drop_duplicates() to get unique countries.
• Created country_name_df: dataframe of unique country names only.
• Created year_df: dataframe containing the Year column.
• Created country_name_and_year_range_df: combined country names with year data (187 unique country–year entries).
• Implemented get_country_data(df, country, start_year, end_year): filters the dataframe by country and year range (inclusive); returns matching rows.
• Tested get_country_data() with country "Czechia", start_year 1930, end_year 2020; returned 1 row (Czechia, 1940).

-------------------------------------------------------------------------------
FILES MODIFIED / ADDED
-------------------------------------------------------------------------------
• Project 2.ipynb — Data import, cleaning, and filtering logic added (Steps 1 and 2).
• Version Control.txt — This summary (08/02/2026).

-------------------------------------------------------------------------------
NOTES
-------------------------------------------------------------------------------
• Cell 2 uses variable "df" in the dropna assignment; ensure the cleaned result is assigned to main_df (e.g. main_df = main_df.dropna(...)) so later cells use the cleaned data consistently.
• Cell 4 references "df" when building country_name_df; should align with the same dataframe variable (main_df) used after cleaning.

CHANGES COMPLETED
-------------------------------------------------------------------------------
11-2-2026
Step 3 — Logical filtering and visualisation
- Filtered main_df for countries with CO2 Emissions Per Capita > 10 metric tonnes in 2020.
- Created country_year_and_co2_filtered: filtered dataset containing countries meeting the emission threshold.
- Dropped "Annual Temperature Anomaly" and "Country Code" columns from filtered results for cleaner output.
- visualised filtered data using matplotlib bar chart: x-axis = "Country Name", y-axis = "CO2 Emissions Per Capita".
- Chart titled "Countries where CO2 Emissions are above 10 metric tonnes in 2020" with y-axis labeled "Tonnes".
- Created country_co2_and_temperature_df: subset of main_df with "Country Code" and "Year" columns removed, retaining "Country Name", "CO2 Emissions Per Capita", and "Annual Temperature Anomaly".

Step 4 — Probability and summary statistics
- Created annual_temperature_and_year_df: subset of main_df containing only "Year" and "Annual Temperature Anomaly" columns (dropped "Country Name", "Country Code", and "CO2 Emissions Per Capita").
- Implemented get_mean_temperature_data(df): calculates mean temperature anomaly for each year across all countries using groupby() and mean().
- Function returns dataframe with columns renamed to "Year" and "Mean Temperature".
- Tested get_mean_temperature_data() on annual_temperature_and_year_df; successfully computed yearly mean temperature anomalies.

-------------------------------------------------------------------------------
NOTES
-------------------------------------------------------------------------------
- Step 3 includes test code (main_df.drop(columns=[...])) that displays temporary outputs without reassignment; final dataframes (country_year_and_co2_filtered, country_co2_and_temperature_df) are correctly assigned.
- matplotlib.pyplot imported as plt for visualization in Step 3.
- get_mean_temperature_data() function uses .reset_index() to convert grouped data back to dataframe format with explicit column naming.
- Consider verifying that main_df remains unmodified throughout Steps 3 and 4, as multiple drop() operations were performed for testing and subset creation.
